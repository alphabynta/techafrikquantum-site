<svg class="logo-svg" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 130 44" role="img" aria-label="TechAfrik logo" style="width: clamp(120px, 15vw, 170px); height: auto; display: block;">

  <!-- ── SIGNAL ARCS ICON ── -->
  <g fill="none" stroke="#f5a524" stroke-linecap="round">
    <!-- outer arc -->
    <path d="M6 34 A18 18 0 0 1 30 10" stroke-width="2.2" opacity="0.35"/>
    <!-- middle arc -->
    <path d="M9 34 A13 13 0 0 1 27 14" stroke-width="2.2" opacity="0.65"/>
    <!-- inner arc -->
    <path d="M12 34 A8 8 0 0 1 24 19" stroke-width="2.2"/>
  </g>
  <!-- anchor dot -->
  <circle cx="12" cy="34" r="2.2" fill="#f5a524"/>

  <!-- ── TECH (white, Anurati) ── -->
  <text
    x="38" y="16"
    font-family="'Anurati', sans-serif"
    font-size="13"
    font-weight="700"
    fill="#ffffff"
    dominant-baseline="central"
    letter-spacing="2">TECH</text>

  <!-- ── AFRIK (amber, Anurati) ── -->
  <text
    x="38" y="29"
    font-family="'Anurati', sans-serif"
    font-size="13"
    font-weight="700"
    fill="#f5a524"
    dominant-baseline="central"
    letter-spacing="1.2">AFRIK</text>

</svg>
